import { createInitiator } from './_createFlowInitiator';

// Not currently used - redirecting manually instead of initiating
export const get = createInitiator('initializeSelfServiceSettingsFlowForBrowsers');
